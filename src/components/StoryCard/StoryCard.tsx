import React from "react";
import classNames from "classnames";
import classes from "./StoryCard.module.scss";

const StoryCard = () => {
	return <div className={classNames(classes.story_card_main)}>dfsgdsf</div>;
};

export default StoryCard;
